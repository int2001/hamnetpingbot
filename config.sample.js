/* ConfigFile for Ping-Bot */
const config = {
	TELE_TOKEN:'[Insert Token acquired from @BotFather at Telegram here]',
	hostcheck: '[Host or IP of continuous checked host]',
	defaultChat: '[ChatID where to post alarmings]',
	AllowedChats: [Array_wth_allowed, ChatIDs, ACL who can interact with the bot],
};

module.exports = config;
